\subsection{Installation}
To install the client, we have to follow a similar path to the logger installation. Only this time we run the \code{ansible/installClient.yaml} playbook, like so: \code{ansible-playbook install.yaml -l sfs}. This should take care of everything.